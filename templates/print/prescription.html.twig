{% extends 'print/base.html.twig' %}

{% block body %}
    {% set barcode = get_random_barcode() %}
    <div style="padding: 5mm 5mm 0;">
        <h1 class="b" style="font-size: 24px; padding-left: 5mm;">recepta</h1>
    </div>
    <div style="padding: 2mm 5mm 0;">
        <img style="width: 100%; height: 10mm;" src="{{ barcode.file }}" alt="barcode" />
        <p style="letter-spacing: .5px; font-size: 8.7px; margin-top: -2px;">{{ barcode.code }}</p>
    </div>
{% endblock %}
